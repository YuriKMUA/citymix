<% provide(:title, 'Редагування профілю') %>
<h2>Редактирование профиля </h2>

<% if signed_in? %>
   <%= render "edit" %>
<% else %>
   <% flash.now[:danger] = "Вы совершили попытку редактирований профиля без аутентификации" %>
<% end %>
      
